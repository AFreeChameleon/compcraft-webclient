import React from 'react';
import styled from 'styled-components';

type NotepadProps = {
  notepad: any;
}

class Notepad extends React.Component<NotepadProps> {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div></div>
    )
  }
}

export default Notepad;